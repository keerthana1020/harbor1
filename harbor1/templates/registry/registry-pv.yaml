  apiVersion: v1
    kind: PersistentVolume
    metadata:
      name: registry-pv
    spec:
      storageClassName: gp3-csi
      capacity:
        storage: 5Gi
      accessModes:
        - ReadWriteOnce
      hostPath:
        path: /data/registry
